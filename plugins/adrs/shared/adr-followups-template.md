# 🗂️ Follow-ups — Short title

> ADR **YYYYMMDD-title** · this is the **follow-ups** list.
> Related: [summary](YYYYMMDD-title-0-summary.md) · [plan](YYYYMMDD-title-1-plan.md).
>
> This file tracks **follow-up action items that fall outside the essential
> implementation** — which the [plan](YYYYMMDD-title-1-plan.md) covers. Use it for
> anything worth remembering but not part of the core build: bugs found, gaps,
> improvements, and nice-to-haves. `adrs:implement` does **not** build these;
> they are tracked here for later.
>
> Items are grouped into **Bugs** (`B`), **Gaps** (`G`), **Improvements** (`I`),
> and **Nice-to-have** (`N`), each with an ID (`B1`, `G2`, …). The Overview table
> lists every item; each group section holds a detail entry under
> `### <ID> — <Title>` with **Description**, **Source** (`human` / `review` /
> `agent`), **Status** (`accepted` = a follow-up worth doing / `out-of-the-scope`
> = declined), and an optional **Status reason**. Every overview row must have a
> matching detail entry and vice versa, and their Source/Status must agree. Leave
> a group empty when it has no items. Keep this file under **350 lines**.

## 📊 Overview

| Group | ID | Title | Source | Status |
| ----- | -- | ----- | ------ | ------ |

## 🐛 Bugs

<!-- Example entry — copy this shape for each bug and add a matching Overview row:

### B1 — Short bug title

**Description:** What breaks, when, and why it matters. Repro steps, links, or
code references can go here across multiple lines.

**Source:** review

**Status:** accepted

**Status reason:** accepted as a bug because it causes connection exhaustion under load.
-->

## 🕳️ Gaps

## 📈 Improvements

## 🌟 Nice-to-have
