# 🗂️ Backlog — Short title

> ADR **YYYYMMDD-title** · this is the **backlog** (the action items).
> Related: [summary](YYYYMMDD-title-summary.md) · [plan](YYYYMMDD-title-plan.md).
>
> Action items are grouped into **Bugs** (`B`), **Gaps** (`G`),
> **Improvements** (`I`), and **Nice-to-have** (`N`). Each item has an ID
> (`B1`, `G2`, …). The Overview table lists every item; each group section holds
> a detail entry per item under `### <ID> — <Title>` with **Description**,
> **Source** (`human` / `review` / `agent`), **Status** (`accepted` /
> `out-of-the-scope`), and an optional **Status reason**. Every overview row must
> have a matching detail entry and vice versa, and their Source/Status must
> agree. Leave a group empty when it has no items. Keep this file under **350
> lines**.

## 📊 Overview

| Group | ID | Title | Source | Status |
| ----- | -- | ----- | ------ | ------ |

## 🐛 Bugs

<!-- Example entry — copy this shape for each bug and add a matching Overview row:

### B1 — Short bug title

**Description:** What breaks, when, and why it matters. Repro steps, links, or
code references can go here across multiple lines.

**Source:** review

**Status:** accepted

**Status reason:** accepted as a bug because it causes connection exhaustion under load.
-->

## 🕳️ Gaps

## 📈 Improvements

## 🌟 Nice-to-have
